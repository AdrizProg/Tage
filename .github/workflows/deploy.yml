name: deploy

on:
  push: 
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to VPS
    runs-on: ubuntu-22.04
    steps:
      - name: Deploy
        uses: appleboy/ssh-action@v0.1.8
        with:
          host: 161.22.40.230
          port: 2222
          username: illian
          key: ${{secrets.PRIVATE_KEY}}
          script: "cd /var/www/html/tage2;
          git pull;
          npm run build;
          "